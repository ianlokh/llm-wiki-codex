# Reports

Markdown handoff logs kept from the ingestion and linting roles: changed paths, provenance recorded, unresolved gaps, and lint findings (blocking vs advisory). Kept outside `wiki/` so they never become query evidence, but saved so a closed-world corpus stays auditable.

Suggested filename: `ingest-YYYY-MM-DD.md` or `lint-YYYY-MM-DD.md`.
